(menhir
  (flags --explain --fixed-exception --no-dollars)
  (modules parser))

(library
 (name roc)
 (modules syntax lexer parser util)
 (inline_tests)
 (preprocess
  (pps ppx_inline_test sedlex.ppx)))
