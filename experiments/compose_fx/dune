(menhir
  (flags --explain --fixed-exception --no-dollars)
  (modules parser))

(library
 (name compose_fx)
 (modules compose_fx syntax lexer parser canonical solve ir lower util)
 (libraries language sedlex menhirLib)
 (inline_tests)
 (preprocess
  (pps ppx_deriving.show ppx_inline_test sedlex.ppx)))
