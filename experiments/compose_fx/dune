(menhir
  (flags --explain --fixed-exception --no-dollars)
  (modules parser))

(library
 (name compose_fx)
 (modules compose_fx syntax lexer parser util)
 (libraries language sedlex menhirLib)
 (inline_tests)
 (preprocess
  (pps ppx_inline_test sedlex.ppx)))
